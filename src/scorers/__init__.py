# Scorers package — placeholder for Atharv's half
# This package will contain the scoring modules (semantic, structural, behavioral, integrity)
# that consume artifacts produced by the data pipeline (Tanay's half).
#
# Atharv's code reads ONLY from artifacts/ and data/ — no coupling to src/ modules.
